---
layout: default
pagination:
  data: collections.categories.{{category}}
  size: 10
  alias: category
permalink: "/category/{{ category | slugify }}/index.html"
eleventyComputed:
  title: "Category: {{ category }}"
---

{% assign posts = collections.post | filterByCategory: category %}

<header>
  <h2>{{ title }}</h2>
</header>

<p>All posts for a single category, in reverse chronological order.</p>

{% for post in posts reversed %}
  <div>
    <a href="{{post.url}}">{{ post.data.title }}</a>, posted {{ post.date | niceDate }}
    {% excerpt post %}
  </div>
{% endfor %}